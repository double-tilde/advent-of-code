package main

import (
	"fmt"
)

// Get a slice of each line, which will be []int
// on each slice, do the following checks
//    return if its not all incrementing
//    return if its not all decrementing
//    return if s[i] is over 3 away from s[i-1]
//    else it is valid
// Then add up how many safe strings there are

func seconfProblem() {
	fmt.Println("Problem 2:")
}
